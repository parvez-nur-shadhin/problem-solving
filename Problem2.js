console.log("Problem 2:");
// Problem: Calculate simple interest using the formula SI = (P × R × T) / 100.

let p = window.prompt("Enter The P:");
let r = window.prompt("Enter The R:");
let t = window.prompt("Enter The T:");

let sI = (p * r * t) / 100;

console.log("simple Interest =", sI);